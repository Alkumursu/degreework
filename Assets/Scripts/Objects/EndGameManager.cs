using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EndGameManager : MonoBehaviour
{
    //public EndGameCharacterChecker[] characters;
    /*
    bool isActivated = false;

    void Start()
    {
        
    }

    void Update()
    {
        
    }

    public void CheckCharacters()
    {
        bool failure = false;
        //for(int i = 0; i < characters.Length; i++)
        for (int i = 0; i < characters.Length; i++)
        {
            if (characters[i].IsActivated())
            {
                //lisää teksti jossa vaaditaan molempia hahmoja
            }
            else
            {
                failure = true;
            }
        }

        if (!failure)
        {
            Debug.Log("Game ends");
            //FindObjectOfType<GameManager>().HandleGameWon();
        }

        else
        {
            Debug.Log("Game didn't end");
        }
    }
    */
}
